json.extract! translation, :id, :lan, :tran_title, :descrip, :blkc, :subc, :senc, :xcrip, :li, :pubdate, :version, :message_id, :created_at, :updated_at
json.url translation_url(translation, format: :json)
